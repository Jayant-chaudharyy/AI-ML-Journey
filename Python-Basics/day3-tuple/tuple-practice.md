DAY-3 Tuple Practice Question 

---10 Tuple Practice Questions---

Create a tuple named ai_config containing three integers: (800, 600, 1024).

Print the second item from ai_config.

Write a line of code that tries to change the first item of ai_config to 900. When you see the TypeError, can you explain in your own words why it happened?

Create a tuple with a single string value: ("gemini",). (Remember the special requirement for single-item tuples!)

Combine your ai_config tuple with another tuple (2048, 4096) and print the result.

Use len() to check how many items are in your ai_config tuple.

Create a tuple of three model names. Use the .index() method to find the position of one of them.

Unpack a tuple (10, 20, 30) into three variables named a, b, and c, then print b.

Convert your ai_config tuple into a list, change the first value to 900, and convert it back into a tuple.

Use del to delete the entire tuple and then try to print it to see what happens.