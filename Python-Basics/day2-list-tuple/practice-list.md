15 Practice Questions

Create a list called ai_models with three model names.

Print the second item in that list.

Add a fourth model to the list using append().

Change the first item in the list to "Gemini-Pro".

Find the length of the list using len().

Remove the last item from the list using pop().

Check if "GPT-4" exists in your list using the in keyword.

Create a second list other_models and combine it with ai_models using the + operator.

Sort your list alphabetically using sort().

Reverse the order of your list using reverse().

Use insert(1, "DeepSeek") to put a model at the second position.

Clear all items from the list using clear().

Create a list of numbers [10, 20, 30, 40, 50] and print the sum using the sum() function.

Use a slice [1:3] to print only the middle two items of a list.

Count how many times the number 20 appears in the list [20, 10, 20, 30, 20] using count().