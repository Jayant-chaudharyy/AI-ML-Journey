---10 Practice Questions---

Create a set named tech_stack with the values {"Next.js", "TypeScript", "Firebase"}.

Try adding a duplicate item (like "Next.js") to the set and print it to prove it only stores it once.

Use the .add() method to add "Python" to your set.

Use the .remove() method to delete "Firebase" from your set.

Check if "TypeScript" exists in your set using the in keyword.

Create two sets: set_a = {1, 2, 3} and set_b = {3, 4, 5}. Find the union of both sets.

Find the intersection (common items) of set_a and set_b.

Find the difference (set_a - set_b) and explain what it returns.

Convert a list [10, 20, 20, 30, 30] into a set to automatically remove the duplicates.

Try to access the first item of your set using tech_stack[0]. What error does Python give you, and why?