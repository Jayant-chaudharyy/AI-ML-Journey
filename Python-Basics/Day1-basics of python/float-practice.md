Create two floats, price1 = 19.99 and price2 = 5.50. Calculate the sum.

Write a program to convert an integer 50 into a float.

Calculate the area of a circle with radius 5.5 (use 3.14 * radius * radius).

Divide 10.0 by 3 and print the result.

Use round() to round 3.14159 to two decimal places.

Multiply a float 0.1 by 3. (Observe why it might not be exactly 0.3—floating-point math is tricky!).

Create a float x = 10.5 and use the -= operator to decrease it by 2.5.

Check if a float 10.5 is equal to the integer 10.

Calculate the average of three floats: 10.5, 20.5, and 30.0.

Find the remainder of 10.5 divided by 3.0.

Convert the string "3.14" into a float.

Check if a float is greater than 0.0.

Multiply a large float (e.g., 1000.0) by a small float (e.g., 0.001).

Print a float in scientific notation using format() or an f-string.

Assign 1.0 to a variable and use the * operator to grow it by 10% (multiply by 1.1)