Assign a string "AI_Developer" to a variable and print only the first character.

Store your full name in a variable. Print it in all uppercase letters using .upper().

Store the string "  python  " (with spaces). Use .strip() to remove the extra whitespace.

Check if the word "model" is inside the string "The model is training".

Write a string that contains both single quotes and double quotes (e.g., He said, "It's time to code!").

Use the + operator to combine "Full" and "Stack" with a dash (-) in between.

Print the last character of any string variable using negative indexing (e.g., variable[-1]).

Convert the integer 2026 to a string and combine it with the text "Year: ".

Use the replace() method to change "Google" to "Meta" in the string "Google AI".

Print a string that spans multiple lines using triple quotes (""").

Use the find() method to locate the index position of the word "python" in a sentence.

Store a sentence and use the split() method to turn it into a list of words.

Print a string repeated 5 times using the * operator.

Use an f-string to insert the variable language = "Python" into the sentence "I am learning X.".

Use the count() method to count how many times the letter "a" appears in "data science".