Create a variable base_price = 100 and discount = 20. Print the total_price.

Write a program to calculate the square of an integer (e.g., 5 to the power of 2) using .

Use the floor division operator (//) to divide 10 by 3.

Use the modulo operator (%) to check if 17 is odd or even.

Create two variables a = 10 and b = 20. Swap their values.

Write an expression that adds 5, multiplies by 2, and then subtracts 3.

Convert the float 15.9 into an integer and print it.

Create a variable day = 1. Use the += operator to increment it by 1.

Find the absolute value of -50 using the abs() function.

Calculate how many seconds are in 2 hours (multiply 2 * 60 * 60).

Compare two integers and print True if the first is strictly larger than the second.

Create a variable total_requests = 1050. Divide it by 100 and use int() to get only the whole quotient.

Raise 2 to the power of 10.

Calculate the remainder of 100 divided by 7.

Define a variable x = 5. Print the result of x > 3 and x < 10.